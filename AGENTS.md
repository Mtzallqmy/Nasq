# تعليمات وكلاء البرمجة — نَسَق (NASAQ)

هذه التعليمات حاكمة لأي وكيل برمجي يعمل داخل هذا المستودع.

## 1. اقرأ قبل أن تكتب الكود

قبل أي تنفيذ، اقرأ بالترتيب:

1. `docs/product/PRODUCT.md`
2. وثيقة المرحلة الحالية داخل `docs/implementation/`
3. أي ADRs داخل `docs/decisions/` عند إنشائها

وثيقة المنتج هي المرجع الأساسي للمتطلبات. لا تخترع متطلبات تجارية تتعارض معها.

## 2. لا تنفذ المنتج كاملًا دفعة واحدة

اعمل مرحليًا. إذا كانت المرحلة الحالية هي Foundation فلا تبدأ وحدات المهام أو المحاسبة أو الديون أو المشاريع كاملة قبل اعتماد الأساس.

## 3. قواعد معمارية غير قابلة للكسر

- Web وMobile لا يتصلان مباشرة بقاعدة البيانات في العمليات الحساسة.
- جميع قواعد العمل والصلاحيات والعمليات المالية تمر عبر Backend/API.
- النظام Multi-user وMulti-workspace من البداية.
- كل بيانات تشغيلية/مالية يجب أن تكون مرتبطة بـ `workspace_id` أو بمسار عزل صريح.
- Authorization يتم Server-side، وليس بمجرد إخفاء عناصر UI.
- الصلاحيات: RBAC + Granular Permissions.
- العربية وRTL هما الأساس، مع بنية i18n قابلة لإضافة الإنجليزية لاحقًا.
- تخزين الزمن يكون بصورة معيارية في UTC، والعرض الافتراضي `Asia/Riyadh`.
- العمليات المالية الجوهرية يجب أن تكون قابلة للتدقيق.
- المحرك المالي المستقبلي يعتمد Double-entry ledger.
- لا حذف صامت لحركة مالية مرحّلة؛ التصحيح/العكس يجب أن يكون موثقًا.
- لا تضع Secrets في المستودع. استخدم `.env.example` فقط للقيم النموذجية.

## 4. جودة التنفيذ

أي مرحلة برمجية يجب أن تنتهي، حسب ما ينطبق عليها، بـ:

- Build ناجح
- Lint ناجح
- TypeScript strict
- اختبارات للمنطق الحرج
- Migrations سليمة
- توثيق القرارات المهمة
- تحديث README أو وثائق المرحلة عند تغير طريقة التشغيل

## 5. القرارات غير المحسومة

إذا واجهت قرارًا غير محسوم قد يؤثر على الأمن أو البيانات أو المعمارية:

- لا تفترض قرارًا خطيرًا بصمت.
- أضفه تحت `OPEN QUESTIONS` في تقرير المرحلة.
- إن احتجت قرارًا معماريًا، أنشئ ADR مناسبًا داخل `docs/decisions/`.

## 6. مبدأ الأمان المالي

إذا تعارضت ميزة مع:

- الأمان المالي،
- عزل مساحات العمل،
- الصلاحيات الدقيقة،
- أو قابلية التدقيق،

فالأولوية لهذه المبادئ حتى لو أدى ذلك إلى تأجيل الميزة.

## 7. تقرير نهاية كل مرحلة

يجب أن يتضمن:

1. ما تم إنشاؤه.
2. الملفات الرئيسية التي تغيرت.
3. القرارات المعمارية.
4. الاختبارات والتحقق المنفذ.
5. ما لم يتم تنفيذه عمدًا.
6. `OPEN QUESTIONS`.
7. المخاطر أو الديون التقنية.
8. اقتراح المرحلة التالية، دون البدء بها تلقائيًا.
