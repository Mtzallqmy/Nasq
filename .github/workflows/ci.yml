name: phase-zero-staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Phase 0 final quality gate is being prepared"
